
    public enum RoomState { Open, Closed, NonVisited }; //Estado de las habitaciones
    public enum EnemyState { Fleeing, Shooting, Idle, Chasing, Stunned };   //Estado de los enemigos, interpretables
    public enum ObjectType { Item, Effect, Weapon } //Tipo de objetos que se pueden recoger
    public enum BulletEffects { None, Piercing, Tactical } //Efetos posibles de la bala.
    public enum Weapons { Default, LenteConvergente, ReactorCinético } //Posibles armas del jugador.